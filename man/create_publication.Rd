% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-publication.R
\name{create_publication}
\alias{create_publication}
\title{Create a publication project}
\usage{
create_publication(
  path = ".",
  title = NULL,
  author = NULL,
  description = "A reproducible publication.",
  chapters = c("Introduction", "Methods", "Results", "Discussion"),
  renv = TRUE,
  git = TRUE,
  github_actions = TRUE,
  branch = "main",
  repo = NULL,
  primary_color = "#0068D9",
  open = rlang::is_interactive()
)
}
\arguments{
\item{path}{Directory to create the project in. Defaults to the working
directory, since R is normally run from the root of the repository that
will hold the publication. The directory may already exist and already
contain files -- a \verb{.git/}, a licence, whatever the repository came with
-- as long as none of them are files this function itself writes.}

\item{title}{Title of the publication. Defaults to a title-cased version of
the directory name.}

\item{author}{Author name. Defaults to \code{getOption("usethis.full_name")}, then
to the \code{user.name} recorded in the local Git configuration.}

\item{description}{One-line description, used in the site metadata and
\code{README.md}.}

\item{chapters}{Character vector of starter chapter titles, in reading order.
A title whose slug matches one of \code{\link[=chapter_templates]{chapter_templates()}} uses that template;
any other title uses the generic \code{"chapter"} template. Pass
\code{character(0)} to create no starter chapters.}

\item{renv}{Whether to initialise a \link[renv:renv-package]{renv::renv} lockfile. \code{TRUE} (the default)
runs \code{renv::init()} in a separate R process, which installs the project's
dependencies into a private library and can take several minutes. \code{FALSE}
skips it and generates a workflow that installs packages unpinned.}

\item{git}{Whether to initialise a Git repository and make an initial commit.}

\item{github_actions}{Whether to write \code{.github/workflows/publish.yml}.}

\item{branch}{Name of the branch that triggers deployment.}

\item{repo}{Optional \code{"owner/name"} GitHub repository, used to add
edit/source links to every page. \code{NULL} omits the links.}

\item{primary_color}{Accent colour for the bs4 theme and the generated CSS.}

\item{open}{Whether to open the new project in RStudio when available.}
}
\value{
The absolute path to the created project, invisibly.
}
\description{
Scaffolds a complete, immediately renderable
\href{https://bookdown.org}{bookdown} publication project: R Markdown sources,
chapter ordering, HTML output configuration, custom CSS, a bibliography, a
Git repository, an optional \link[renv:renv-package]{renv::renv} lockfile, and a GitHub Actions workflow
that deploys the rendered site to GitHub Pages.
}
\details{
The generated project is a standard bookdown project. It renders with
\code{bookdown::render_book()} alone and does not need this package at render
time.
}
\examples{
\dontrun{
create_publication(
  path = "my-analysis",
  title = "My Analysis",
  author = "Jane Doe"
)
}
}
