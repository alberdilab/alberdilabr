% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_publication}
\alias{check_publication}
\title{Validate a publication project}
\usage{
check_publication(project = ".", render = FALSE, quiet = FALSE)
}
\arguments{
\item{project}{Path to the publication project.}

\item{render}{Whether to render the project as a final check. This is slow
and is skipped by default.}

\item{quiet}{Whether to suppress printing. The result is returned either way.}
}
\value{
An object of class \code{publication_check}: a data frame of checks with
columns \code{category}, \code{status} (\code{"pass"}, \code{"warn"} or \code{"fail"}) and
\code{message}. Returned invisibly when \code{quiet} is \code{FALSE}.
}
\description{
Runs a series of structural, document, reproducibility and publishing checks
and reports the result of each one.
}
\details{
Checks are grouped into categories:
\itemize{
\item \strong{Structure} -- required files and directories, readable configuration,
registered chapters that exist, chapter files that are not registered.
\item \strong{Documents} -- exactly one top-level heading per chapter, unique and
well-formed heading identifiers, sequential numbering, duplicate knitr
chunk labels, figures that cannot be cross-referenced.
\item \strong{Reproducibility} -- presence of \code{renv.lock} and whether the packages
needed to render are recorded in it. No network access is performed.
\item \strong{Publishing} -- presence of the workflow, and agreement between the
directory bookdown writes and the directory the workflow uploads.
\item \strong{Rendering} -- only when \code{render = TRUE}, a full build as an integration
test.
}
}
\examples{
\dontrun{
check_publication()
check_publication(render = TRUE)
}
}
