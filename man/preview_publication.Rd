% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{preview_publication}
\alias{preview_publication}
\title{Preview a publication locally}
\usage{
preview_publication(
  project = ".",
  port = NULL,
  browse = rlang::is_interactive(),
  ...
)
}
\arguments{
\item{project}{Path to the publication project.}

\item{port}{Port to serve on. \code{NULL} lets \pkg{servr} choose a free one.}

\item{browse}{Whether to open a browser window.}

\item{...}{Further arguments passed to \code{bookdown::serve_book()}.}
}
\value{
The value returned by \code{bookdown::serve_book()}, invisibly.
}
\description{
Renders the project and serves it on a local web server that rebuilds and
reloads the page whenever a source file changes.
}
\details{
This wraps \code{bookdown::serve_book()}, which is bookdown's own live-preview
server (built on \pkg{servr}). It re-renders only the chapter you edit, which
makes it much faster than a full \code{\link[=render_publication]{render_publication()}} for day-to-day
writing, at the cost of occasionally stale cross-references. Do a full render
before publishing.
}
\examples{
\dontrun{
preview_publication()
}
}
